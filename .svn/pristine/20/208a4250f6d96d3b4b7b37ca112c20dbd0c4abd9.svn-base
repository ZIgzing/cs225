#include "exam.h"
#include "matrix.h"
#include <cstdlib>
#include <iostream>

using namespace std;
using namespace exam;


Matrix exam::flip_columns(const Matrix &m){
  Matrix *Newmatrix = new Matrix();
  *Newmatrix = m;
  for(int i = 0  ; i < Newmatrix->get_num_columns() ; i++){
  for(int j =0 ; j < Newmatrix->get_num_rows(); j++){
  return m;
}
}
}
